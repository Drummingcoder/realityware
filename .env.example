# Shared Database Configuration
# Both Prisma (web) and Diesel (backend) use this connection
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/dev"

# OAuth Configuration (for Rocket backend)
OAUTH_CLIENT_ID=""
OAUTH_CLIENT_SECRET=""
OAUTH_REDIRECT_URI="http://localhost:8000/oauth/callback"

# Rocket Secret Key (generate with `openssl rand -base64 32`)
ROCKET_SECRET_KEY=""
